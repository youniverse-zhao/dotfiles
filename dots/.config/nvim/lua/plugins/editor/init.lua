local gitsigns = require("plugins.editor.gitsigns")
local pairs = require("plugins.editor.pairs")
local telescope = require("plugins.editor.telescope")
local which_key = require("plugins.editor.which-key")

return {
  gitsigns,
  pairs,
  telescope,
  which_key,
}
