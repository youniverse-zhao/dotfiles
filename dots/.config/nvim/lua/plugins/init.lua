local colourschemes = require('plugins.colourschemes')
local editor = require('plugins.editor')
local lsp = require('plugins.lsp')
local ui = require('plugins.ui')

return {
    colourschemes,
    editor,
    lsp,
    ui,
}